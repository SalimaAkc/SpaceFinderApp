<Window x:Class="SpacefinderApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Spacefinder - Login" 
        Height="500" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="#FF2D2D30">
        <Border Background="#FF252526" 
                CornerRadius="10"
                Width="350"
                Height="450"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <StackPanel Margin="30" VerticalAlignment="Center">

                <!-- Logo/App Name -->
                <TextBlock Text="Spacefinder" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Email/Password Form -->
                <TextBlock Text="Email" Foreground="White" Margin="5,0"/>
                <TextBox x:Name="EmailTextBox" Height="35" Margin="0,5,0,15"/>

                <TextBlock Text="Password" Foreground="White" Margin="5,0"/>
                <PasswordBox x:Name="PasswordBox" Height="35" Margin="0,5,0,20"/>

                <!-- Single, styled Login Button -->
                <Button x:Name="LoginButton" 
                        Content="Login" 
                        Height="40" 
                        Margin="0,10"
                        Background= "#007ACC" 
                        Foreground="White"/>

                <!-- Social Login Divider -->
                <Grid Margin="0,20,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Line Grid.Column="0" Stroke="Gray" Stretch="Fill" Y2="1" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="OR" Foreground="Gray" Margin="5,0"/>
                    <Line Grid.Column="2" Stroke="Gray" Stretch="Fill" Y2="1" VerticalAlignment="Center"/>
                </Grid>

                <!-- Social Login Buttons -->
                <Button x:Name="GoogleLoginButton" 
                        Height="40" 
                        Margin="0,5"
                        Background="#2F2F2F"
                        Foreground="White"
                        Click="GoogleLoginButton_Click">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="/Assets/google-logo.png" Width="20" Height="20" Margin="0,0,10,0"/>
                                <TextBlock Text="Sign in with Google" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button x:Name="MicrosoftLoginButton" 
                        Height="40" 
                        Margin="0,5"
                        Background="#2F2F2F"
                        Foreground="White"
                        Click="MicrosoftLoginButton_Click">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Image Source="/Assets/microsoft-logo.png" Width="20" Height="20" Margin="0,0,10,0"/>
                                <TextBlock Text="Sign in with Microsoft" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!-- Register Link -->
                <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Hyperlink Foreground="#FF007ACC">
                        Don't have an account? Register
                    </Hyperlink>
                </TextBlock>

                <!-- Error Message -->
                <TextBlock x:Name="ErrorMessage" 
                           Foreground="Red" 
                           Margin="0,15,0,0" 
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
